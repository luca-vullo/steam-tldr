{
  "name": "steam-tldr",
  "version": "0.1.0",
  "private": true,
  "description": "Estensione Chrome che genera un TL;DR delle recensioni recenti di un gioco sulla sua pagina Steam",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
